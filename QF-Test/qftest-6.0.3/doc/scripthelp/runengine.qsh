/**
 @script common
 @context RunEngine
 @description 
 */
class RunEngine{
    
    /** */
    runAndroid(languageName, cmd, timeout=5000)
    /** */
    runAWT(languageName, cmd, timeout=5000)
    /** */
    runSWT(languageName, cmd, timeout=5000)
    /** */
    runWeb(languageName, cmd, timeout=5000)
    /** */
    runFX(languageName, cmd, timeout=5000)
    /** */
    runWin(languageName, cmd, timeout=5000)
    /** */
    runCmd(languageName, cmd)
}
